var react = {
	parameter: (x,y) => (2*(x+y)),
	area: (x,y) => (x*y)
	}

var solveReact((l,b) => {
	console.log('wridth od reactangle is l: '+ b + ' length is: '+ l);
	console.log('parameter of rectangle is :'+ react.parameter(l,b));
	console.log('area of parameter is :'+ react.area(l,b));
	})

solveReact(10,10);

